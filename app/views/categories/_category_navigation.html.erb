<% if current_user.role == 'admin' %>
  <%= link_to t('categories.all_categories'), categories_path %>
  |
  <%= link_to t('categories.new_category'), new_category_path %>
  |
<% end %>
<%= link_to t('posts.all_articles'), posts_path %>