<%= render 'blog_navigation' %>
<h1><%= @post.title %></h1>
<%= link_to t('posts.update'), edit_post_path(@post) %>
|
<%= link_to t('posts.delete'), post_path(@post), method: :delete, data: { confirm: t('posts.are_you_sure') } %>
<p><%= @post.body %></p>